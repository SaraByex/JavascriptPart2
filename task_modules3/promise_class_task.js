// create a promise to simulate  a food ordering system
// You want to simulate this process:
// User places an order.
// It takes 2 seconds to confirm the order.
// Then it takes 3 seconds to cook the food.
// Finally, a message is shown: "Order is ready!"

// let orderConfirmed = true;

// function placeOrder() {
//     const myPromise = new Promise((resolve, reject) => {
//         setTimeout(() => {
//             if (orderConfirmed === true) {
//             resolve("Order confirmed, processing order");
//         } else {
//             reject("Unable to process your order. Please try again later");
//         }
//     }, 2000)
// }) 
//     return myPromise;
// }


// function cookFood() {
//         myPromise = new Promise((resolve, reject) => {
//             console.log("cooking the food");
//             setTimeout(() => {
//              resolve("order is ready");
//         }, 3000);
//     })
//     return myPromise;
// }
//         placeOrder()
//         .then((result) => console.log(result))           
//         .catch((reject) => console.error(reject))

//         cookFood() 
//         .then((result) => console.log(result))           
//         .catch((reject) => console.error(reject))

    

// const myName = "Kseniia";
// const myPromise = new Promise((resolve, reject) => {
//     //setTimeout (()=>{
//         if(myName === "Kseniia") {
//             resolve("Kseniia is a student of MCA")
//         }else{
//             reject("Kseniia is not a student of MCA")
//         }
//     //}, 3000)

// })

// // console.log(myPromise)
// // function print(result){
// //     console.log(result)

// myPromise
//     .then((result) => console.log(result))
//     .catch((reject) => console.error(reject))

function promise_shower
    let shower = new Promise();
    setTimeout(function(){
        resolve(`2. Shower: ${new Date().getSeconds`)
    }, 3000)
})
return shower;
)
function promise_husband_cooks(){
      let coooks = new Promise
      setTimeout(function(){
       resolve(`2. Shower: ${new Date().getSeconds)`)
    }, 5000)
    return cooks
})

    function promise_husband_cooks(){
      let coooks = new Promise
      setTimeout(function(){
       resolve(`2. Shower: ${new Date().getSeconds`)
    }, 5000)

    function promise_get_dressed(){
      let dressed = new Promise
      setTimeout(function(){
       resolve(`2. Shower: ${new Date().getSeconds`)
    }, 5000)
   
    console.log(1wake up at: $())

    promise_husband_cooks()
    .then(result)=>
        console.log("eat breakfast")
        return promise_eat_breakfast;

    .then(result)=>
        console.log(result)
    return promise_get_dressed(;
    
    )
